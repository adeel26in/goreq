Goreq

A simple HTTP/HTTPS requester wriiten in Go. Just download the prebuilt binaries or if you wanna build from source:

git clone github.com/adeel26in/goreq

cd goreq/src

go build # or if you don't wanan build a binary just run go run .
